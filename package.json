{
  "name": "@yoganataa/naru-agents",
  "version": "0.0.2",
  "description": "N.A.R.U. - Next-gen Autonomous Role-based Unified agents orchestration for OpenCode with 10 role-based subagents and RAG grounding",
  "type": "module",
  "bin": {
    "naru": "./bin/naru-agents.js",
    "naru-agents": "./bin/naru-agents.js"
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0"
  },
  "files": [
    "bin/",
    "src/",
    "agents/",
    "knowledge/",
    "install.sh"
  ],
  "scripts": {
    "test": "node bin/naru-agents.js validate",
    "docs:preview": "node src/docs-preview.mjs",
    "build:bin": "bun build --compile --minify ./bin/naru-agents.js --outfile ./dist/naru.exe",
    "build:all": "bun build --compile --minify ./bin/naru-agents.js --target=bun-windows-x64 --outfile=./dist/naru-windows.exe && bun build --compile --minify ./bin/naru-agents.js --target=bun-linux-x64 --outfile=./dist/naru-linux && bun build --compile --minify ./bin/naru-agents.js --target=bun-darwin-arm64 --outfile=./dist/naru-macos"
  },
  "keywords": [
    "opencode",
    "ai",
    "agent",
    "naru",
    "orchestration",
    "subagent",
    "bun"
  ],
  "author": "yoganataa",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yoganataa/naru-agents.git"
  },
  "bugs": {
    "url": "https://github.com/yoganataa/naru-agents/issues"
  },
  "homepage": "https://github.com/yoganataa/naru-agents#readme",
  "devDependencies": {
    "marked": "^18.0.10"
  },
  "dependencies": {
    "@opentui/core": "^0.5.6"
  }
}
